'use client';

/**
 * D tier theme — "low normie". Warm orange dust wash. Cooler than
 * chopped, calmer.
 */
export default function ThemeLowNormie() {
  return (
    <div
      aria-hidden
      className="pointer-events-none fixed inset-0 -z-10"
      style={{
        background:
          'radial-gradient(ellipse at 50% 25%, #9a3412 0%, #431407 60%, #0a0a0a 110%)',
      }}
    />
  );
}
