'use client';

/**
 * C tier theme — "normie". Amber-gold neutral wash, plain and content.
 * The "perfectly average" theme.
 */
export default function ThemeNormie() {
  return (
    <div
      aria-hidden
      className="pointer-events-none fixed inset-0 -z-10"
      style={{
        background:
          'radial-gradient(ellipse at 50% 30%, #854d0e 0%, #422006 65%, #0a0a0a 115%)',
      }}
    />
  );
}
